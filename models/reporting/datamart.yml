version: 2

models:
  - name: yelp_tips_datamart
    description: Datamart for Tips Analyses
    columns:
      - name: tps_sec_state
        description: Security column on state
        meta:
          row_access_policy: state_policy

  - name: yelp_dm_d_users
    description: Datamart for User Dimension
    columns:
      - name: usr_sec_state
        description: Security column on state
        meta:
          row_access_policy: state_policy